/* eslint-disable @typescript-eslint/no-require-imports */

// Metro requires a literal CommonJS reference to bundle a static native asset.
export const aiyomiMark = require("../../assets/brand/aiyomi-mark.png") as number;
